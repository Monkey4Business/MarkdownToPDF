\documentclass[$if(fontsize)$$fontsize$$endif$]{article}

% -----------------------------
% Layout
% -----------------------------
\usepackage[margin=2.0cm]{geometry}

% Header spacing
\setlength{\headheight}{34pt}
\setlength{\headsep}{22pt}

% Paragraphs
\usepackage{ragged2e}
\RaggedRight
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% -----------------------------
% Language
% -----------------------------
\usepackage[ngerman]{babel}

% -----------------------------
% Microtype (XeLaTeX-safe)
% - Only protrusion is reliably supported with XeLaTeX.
% - Disable pdftex-only features (kerning/spacing) to avoid fatal errors.
% -----------------------------
%\usepackage[
%  final,
%  protrusion=true,
%  expansion=false,
%  tracking=false,
%  kerning=false,
%  spacing=false
%]{microtype}

% -----------------------------
% Colors + Graphics
% -----------------------------
\usepackage{xcolor}
\usepackage{graphicx}

% Brand colors (KEEP THESE NAMES STABLE)
\definecolor{MFourBBlue}{HTML}{1A365D}
\definecolor{MFourBOrange}{HTML}{FF6B35}
\definecolor{MFourBTurquoise}{HTML}{2EC4B6}
\definecolor{MFourBGrey}{HTML}{4A4E69}
\definecolor{MFourBLightGrey}{HTML}{F5F5F5}

% Table hairline color (print-friendly)
\definecolor{MFourBRowLine}{HTML}{E6E6E6}

% Hairline inserted by add-mainrow-rules.pl
\newcommand{\MFourBHairline}{%
  \noalign{%
    \color{MFourBRowLine}\hrule height 0.25pt%
    \color{black}%
  }%
}

% -----------------------------
% Fonts (XeLaTeX)
% -----------------------------
\usepackage{fontspec}

$if(mainfont)$
\setmainfont[
  UprightFont    = *-Regular,
  ItalicFont     = *-Italic,
  BoldFont       = *-SemiBold,
  BoldItalicFont = *-SemiBoldItalic
]{$mainfont$}
$endif$

$if(sansfont)$
\setsansfont[
  UprightFont    = *-Regular,
  ItalicFont     = *-Italic,
  BoldFont       = *-Bold,
  BoldItalicFont = *-Bold
]{$sansfont$}
$endif$

$if(monofont)$
\setmonofont{$monofont$}
$endif$

% -----------------------------
% Headings typography + Notfall-Puffer NUR für Überschriften
% -----------------------------
\usepackage{titlesec}

\newcommand{\MFourBHeadingFlex}{%
  \pretolerance=2000\relax
  \tolerance=3000\relax
  \emergencystretch=2em\relax
  \hyphenpenalty=10000\relax
  \exhyphenpenalty=10000\relax
  \sloppy
}

\titleformat{\section}
  {\MFourBHeadingFlex\Large\bfseries\color{MFourBBlue}}
  {\thesection}{0.8em}{}

\titleformat{\subsection}
  {\MFourBHeadingFlex\large\bfseries\color{MFourBBlue}}
  {\thesubsection}{0.8em}{}

\titleformat{\subsubsection}
  {\MFourBHeadingFlex\normalsize\bfseries\color{MFourBBlue}}
  {\thesubsubsection}{0.8em}{}

\titlespacing*{\section}{0pt}{2.0ex plus 0.5ex}{1.2ex}
\titlespacing*{\subsection}{0pt}{1.6ex plus 0.4ex}{1.0ex}
\titlespacing*{\subsubsection}{0pt}{1.2ex plus 0.3ex}{0.8ex}

% -----------------------------
% Hyperlinks
% -----------------------------
\usepackage{xurl}
\usepackage{hyperref}

$if(linkcolor)$
\definecolor{MFourBLink}{HTML}{$linkcolor$}
$else$
\definecolor{MFourBLink}{HTML}{4A4E69}
$endif$

\hypersetup{
  hidelinks,
  colorlinks=true,
  linkcolor=MFourBLink,
  urlcolor=MFourBLink,
  citecolor=MFourBLink
}

% -----------------------------
% Tables
% -----------------------------
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{colortbl}
\usepackage{makecell}
\usepackage{calc}
\usepackage{caption}
\usepackage{etoolbox}

\setlength{\LTpre}{12pt}
\setlength{\LTpost}{4pt}

\setlength{\heavyrulewidth}{0.8pt}
\setlength{\lightrulewidth}{0.3pt}
\setlength{\cmidrulewidth}{0.3pt}

\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.25}

\AtBeginEnvironment{longtable}{\small}
\AtBeginEnvironment{tabular}{\small}
\AtBeginEnvironment{tabularx}{\small}

\captionsetup{labelfont=bf,font=small,skip=8pt}

\newcolumntype{P}[1]{>{\RaggedRight\arraybackslash}p{#1}}

% -----------------------------
% Header / Footer
% -----------------------------
\usepackage{fancyhdr}

\fancypagestyle{mainstyle}{
  \fancyhf{}
  \fancyhead[L]{\includegraphics[height=1cm]{monkey4business_logo_quer.png}}
  \fancyhead[R]{\raisebox{0.25cm}{\textcolor{MFourBBlue}{\footnotesize\textit{$title$}}}}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0.4pt}
  \renewcommand{\footrulewidth}{0.4pt}
}

\fancypagestyle{plain}{\pagestyle{mainstyle}}
\pagestyle{mainstyle}

% -----------------------------
% Pandoc header-includes
% -----------------------------
$if(header-includes)$
$for(header-includes)$
$header-includes$
$endfor$
$endif$

\begin{document}

$body$

\end{document}
